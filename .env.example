# Django
SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Database engine: "postgres" (default/strict) or "sqlite" (local fallback)
DB_ENGINE=sqlite

# PostgreSQL settings (used when DB_ENGINE=postgres)
DB_NAME=togo_rail
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=127.0.0.1
DB_PORT=5432
